# erb-disable-minification-exmaple

## Setup

```bash
git clone https://github.com/amilajack/erb-disable-minification-exmaple
cd erb-disable-minification-exmaple
git checkout v0.14.0
yarn
yarn build
```
